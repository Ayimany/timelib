#ifndef TIME_HH
#define TIME_HH

namespace preclib { namespace time {

enum TimeUnit {
    HOURS =        3600000000,
    MINUTES =      60000000,
    SECONDS =      1000000,
    MILISECONDS =  1000,
    MICROSECONDS = 1,
};

}}

#endif // TIME_HH
